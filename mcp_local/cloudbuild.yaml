steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/ministry-data-mcp'
      - '-f'
      - 'mcp_local/Dockerfile'
      - '.'

  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/ministry-data-mcp'

images:
  - 'gcr.io/$PROJECT_ID/ministry-data-mcp'

timeout: '600s'

